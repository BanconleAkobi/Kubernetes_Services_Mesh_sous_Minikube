// Mode mock activé par défaut pour le développement local.
// En production (conteneur), ce fichier est regénéré par docker-entrypoint.sh
// à partir des variables d'environnement SAMPLE_API_URL et ANALYSIS_API_URL.
window.LABOTRACK_CONFIG = {
    mockMode: true
};
