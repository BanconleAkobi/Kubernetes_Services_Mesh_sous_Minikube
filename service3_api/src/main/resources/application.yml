spring:
  application:
    name: tpArchiStep2_Service3
  datasource:
    url: jdbc:h2:file:/data/tpArchiStep2
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true

server:
  port: 9002
